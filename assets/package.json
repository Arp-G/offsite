{
  "devDependencies": {
    "autoprefixer": "^10.4.2",
    "postcss": "^8.4.5",
    "tailwindcss": "^3.0.12"
  },
  "scripts": {
    "install-node-deps": "npm install --global command-line-args transmission",
    "install-binaries": "apt-get -y install transmission-daemon && apt-get -y install zip",
    "deploy": "npm run install-node-deps && npm run install-binaries && NODE_ENV=production tailwindcss --postcss --minify -i css/app.css -o ../priv/static/assets/app.css && cd .. && mix assets.deploy"
  },
  "dependencies": {
    "postcss-import": "^14.0.2"
  }
}
