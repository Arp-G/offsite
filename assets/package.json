{
  "devDependencies": {
    "autoprefixer": "^10.4.2",
    "postcss": "^8.4.5"
  },
  "scripts": {
    "install-node-deps": "npm install --global command-line-args transmission",
    "deploy": "npm run install-node-deps && cd .. && mix assets.deploy"
  },
  "dependencies": {
    "postcss-import": "^14.0.2"
  }
}
